version: "3.9"
services:
  app-static:
    container_name: portfolio-teste
    image: nginx:alpine
    ports:
      - "3000:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
    restart: always
  

